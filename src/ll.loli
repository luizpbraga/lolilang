




list := [1, 2, 3, 4, 5, 6, 7, 8]

fn find(x, n) {
  len := x.len
  odd := len % 2 == 0
  idx := if odd { len / 2 } else { (len - 1) / 2 }

  if list[idx] == n {
      return n
  }

  lhs := list[0..idx]
  rhs := list[idx..len]

  return [lhs, rhs]
}


@print(find(list, 1))
